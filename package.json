{
  "name": "odd-promises",
  "version": "0.0.1",
  "description": "Library to do interesting things with promises, like resolving promises outside of the promise itself.",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "prepack": "npm run build && yarn install --production"
  },
  "keywords": [
    "promises"
  ],
  "author": "Mitch Mommers",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.2.4",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
